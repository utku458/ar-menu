# End-to-end tests answer these origins in the browser. The API shares the dashboard's site (localhost), like
# app.armenu.app and api.armenu.app in production, so its SameSite=Strict refresh cookie works.
VITE_API_BASE_URL=http://localhost:5999
VITE_GUEST_MENU_BASE_URL=https://armenu.test/m/
# The fake storage of the end-to-end tests.
VITE_STORAGE_ORIGINS=http://localhost:5998
